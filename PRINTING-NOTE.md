# Pasha Baby invoice direct print

Safari may append the page URL, date/time, and page number when printing an HTML document. The invoice direct-print path therefore prints a generated PDF document instead of the temporary HTML/image page. This keeps browser headers/footers out of the invoice while preserving the current rendered invoice appearance.
